export function fitTextToWidth(value:string,fontSize:number,maxWidth:number){const avg=fontSize*.52;const maxChars=Math.max(1,Math.floor(maxWidth/avg));return value.length<=maxChars?value:value.slice(0,Math.max(0,maxChars-1))+"…";}
export function wrapTextToWidth(value:string,fontSize:number,maxWidth:number){const avg=fontSize*.52;const maxChars=Math.max(1,Math.floor(maxWidth/avg));const words=value.replace(/\s+/g," ").trim().split(" ").filter(Boolean);const lines:string[]=[];let current="";for(const word of words){const next=current?`${current} ${word}`:word;if(next.length<=maxChars){current=next}else{if(current)lines.push(current);current=word}}if(current)lines.push(current);return lines;}
